import {
  detectPosesInVideo as detectPosesInVideoImpl,
  analyzePoseData as analyzePoseDataImpl,
} from "@/lib/pose-detection"

export const detectPosesInVideo = detectPosesInVideoImpl
export const analyzePoseData = analyzePoseDataImpl
